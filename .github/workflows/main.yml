name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Build Application
        run: |
          # Your build commands here

      - name: Install sshpass
        run:  sudo apt-get install -y sshpass

      - name: Deploy to cPanel
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" scp -P ${{ secrets.SSH_PORT }} -r ${{ github.workspace }}/path/to/local/files/* ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.REMOTE_DIR }}
        env:
          SSH_USER: root
          SSH_HOST: 45.56.223.102
          SSH_PORT: 22
          REMOTE_DIR: /home/peern270/portal.peernetics.io
          SSH_PASSWORD: c720JPUqz93nV1yGmL
